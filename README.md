# EHANK

This repository contains code and data for the project on the EHANK framework.

## 👥 Authors
- Boris Chafwehe
- Francesca Diluiso
- Guillaume Schwegler

## 📁 Folder Structure

- `data/` – Raw and processed data (raw data is excluded from Git)
- `code/` – Source code, organized by language (MATLAB, Stata, R, Python, etc.)
- `output/` – Figures, tables, results
- `manuscript/` – LaTeX paper source
- `logs/` – (Optional) Logs and temporary files

## 🛠️ Requirements
- Python 3.12 with packages from `requirements.txt`

## 🚀 Getting Started

To clone the repo:

```bash
git clone git@github.com:yourusername/EHANK.git

To recreate SSJ environment (all the packages necessary to run the code):

conda env create -f environment.yml
conda activate ssj-env
